# 闪电贪吃蛇 (Speedy Snake) H5项目完整文档

本文件包含该 H5 游戏的 **产品需求文档 (PRD)** 与 **技术方案文档 (TDD)**，适用于移动端、微信内嵌浏览器及 PC 网页端开发交接。

---

## 📝 一、 产品需求文档 (PRD)

### 1. 项目概述
* **项目名称**：闪电贪吃蛇 (Speedy Snake)
* **目标平台**：移动端 H5 浏览器（微信、Safari、Chrome）、PC 网页端自适应
* **核心玩法**：玩家控制蛇在网格中移动吞噬食物。每吃一个食物长度增加并获得积分，同时移动速度加快。撞击墙壁或自身身体则游戏结束。

### 2. 游戏核心规则
* **网格系统**：游戏核心区域采用 **20 × 20 的等分网格 (Grid)**。
* **死亡判定**：
  * **硬边界碰撞**：蛇头移出 20×20 网格边界（边缘撞墙）。
  * **自残碰撞**：蛇头移入当前自身蛇身（不含蛇尾在移动帧的预判位）占用的网格。
* **食物生成**：
  * 场上同时仅存在 **1 个** 普通食物。
  * 食物坐标完全随机，但**严禁**生成在当前蛇头和蛇身所在的网格。
* **成长与积分**：
  * 成功吞噬食物后，蛇身长度 **+1 节**，当前得分 **+10 分**。
* **速度阶梯机制**：
  * **初始速度**：每步移动间隔为 **200 毫秒**。
  * **动态加速**：每当当前得分增加 **50 分**，移动间隔减少 **15 毫秒**。
  * **速度上限**：最低移动间隔为 **70 毫秒**，达到后不再加速。

### 3. UI 界面与交互流向
* **界面三段式布局**：
  1. **顶部状态栏**：实时显示“当前得分 (Score)”与“历史最高分 (Best Score)”。
  2. **中部游戏区**：正方形 Canvas 游戏画布。
  3. **底部操作区**：仅在移动端/触屏设备显示虚拟十字方向键（▲、▼、◀、▶）。
* **多端操控适配**：
  * **PC 端**：支持键盘方向键（`ArrowUp/Down/Left/Right`）与 `WASD` 键。
  * **移动端（双重操控）**：支持点击底部虚拟十字键转向；同时支持在 Canvas 画布区域执行手势上下左右轻扫（Swipe）转向。
* **数据持久化**：使用浏览器本地缓存（`localStorage`）存储历史最高分，刷新页面不丢失。

---

## 🛠️ 二、 技术方案文档 (TDD)

### 1. 技术栈选型
* **渲染引擎**：原生 HTML5 Canvas API（无第三方引擎依赖，确保极简体积与高加载速度）。
* **逻辑层**：原生 JavaScript (ES6+)。
* **样式适配**：CSS3 Flexbox 弹性盒布局 + Media Queries 媒体查询。

### 2. 核心数据结构设计

#### 2.1 蛇身队列 (Array)
使用数组存储蛇身每个节点的二维网格坐标。数组索引 `0` 固定为蛇头，末尾固定为蛇尾。
```javascript
// 示例：一条长度为 3 节、位于地图中央并向右前进的蛇
let snake = [
  { x: 10, y: 10 }, // 蛇头
  { x: 9,  y: 10 }, // 蛇身
  { x: 8,  y: 10 }  // 蛇尾
];
```

#### 2.2 移动与进食算法流程
游戏主循环（Game Loop）每帧执行以下逻辑：
1. **坐标预判**：根据当前前进方向，计算出下一个潜在的蛇头坐标 `nextX` 和 `nextY`。
2. **碰撞校验**：
   * 检查 `nextX < 0 || nextX >= 20 || nextY < 0 || nextY >= 20` 是否成立（撞墙）。
   * 检查 `snake.some(cell => cell.x === nextX && cell.y === nextY)` 是否成立（撞自身）。
   * 任一成立则调用 `gameOver()` 停止主循环。
3. **节点更新**：
   * 将新头压入队列：`snake.unshift({ x: nextX, y: nextY })`。
   * **分支判定**：
     * 如果 `nextX === food.x && nextY === food.y`（吃食物）：触发得分递增，重新调用 `generateFood()`，**不弹出** 数组尾部。
     * 如果未吃到食物：弹出数组最后一节：`snake.pop()`。

#### 2.3 指令输入缓冲队列 (FIFO Queue)
* **潜在风险**：当蛇向右移动时，玩家在极短时间内（小于一帧的间隔）连续按下“上”和“左”。若直接修改方向变量，蛇的当前方向会在逻辑未更新前变为“左”，导致下一帧移动时蛇头直接逆向撞击自身身体造成 Bug。
* **解决方案**：引入 `inputQueue` 数组。
  * 所有的键盘/触控转向事件不直接修改 `currentDir`，而是执行 `inputQueue.push(dir)`。
  * 在游戏主循环的每帧起始点，使用 `inputQueue.shift()` 仅取出 **1 个** 有效的方向指令进行非法反向过滤，过滤通过后更新 `currentDir`。

### 3. Canvas 渲染优化
* **像素对齐**：Canvas 内部固定分辨率设置为 `400 × 400` 像素，每个网格固定为 `20 × 20` 像素。
* **自适应缩放**：通过 CSS 将 Canvas 宽度设为 `width: 90vw; height: 90vw; max-width: 400px; max-height: 400px;`，由浏览器硬件加速处理缩放，防止移动端画面模糊。
* **渲染性能**：每次重绘前使用 `ctx.fillRect` 清空全屏，避免图层叠加导致的内存泄露。
